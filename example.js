const newVariable = "hello";

console.log(newVariable);

// adding a new line

const tutor = "Kev";

for (let i = 0; i < tutor.length; i++) {
	const char = tutor[i];
	if (char === char.toUpperCase()) {
		console.log(char);
	} else {
		console.log(i);
	}
}


//new line
